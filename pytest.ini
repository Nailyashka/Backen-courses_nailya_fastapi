; [pytest]
; pythonpath = . src
; env_files = .env-test
; asyncio_mode = auto
; asyncio_default_fixture_loop_scope = session
; python_files = test_*.py



; [pytest]
; pythonpath = . src
; asyncio_mode = auto
; # session — один loop на все тесты; на Windows + asyncpg часто даёт "attached to different loop"
; # function — каждый тест в своём loop (медленнее, но стабильно на Windows)
; asyncio_default_fixture_loop_scope = function
; asyncio_default_test_loop_scope = function
; python_files = test_*.py

[pytest]
pythonpath = . src
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
env_files = .env-test
python_files = test_*.py